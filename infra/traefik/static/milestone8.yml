entryPoints:
  web:
    address: ":8080"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

api:
  dashboard: false

ping:
  entryPoint: web

log:
  level: INFO

# Access logging remains disabled because future launch URLs may carry credentials.
