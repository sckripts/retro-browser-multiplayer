entryPoints:
  web:
    address: ":8080"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

api:
  dashboard: false

ping:
  entryPoint: web

log:
  level: INFO

# Access logging is disabled: browser bootstrap requests can contain scoped tokens.
