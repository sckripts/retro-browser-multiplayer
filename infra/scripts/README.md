# Operator scripts

- `production/` is the supported operator interface for initialization, image
  builds, start, verification, and stop.
- `acceptance/` preserves milestone proof and regression scripts. They are for
  development and release qualification, not deployment.

See `docs/deployment.md` for the production procedure.
